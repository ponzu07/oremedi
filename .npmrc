# engines in package.json are advisory (node floor). Kept non-strict so
# contributors on newer Node than better-sqlite3's declared range can still
# install locally; the Docker image pins node:22.
engine-strict=false
